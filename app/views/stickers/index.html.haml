%h1 Stickers list
%ul
  %li= "#{@stickers.count} stickers created"
  %li= "#{@activated.count} stickers activated"
  %li= "#{@verified.count} stickers verified"
%br
%p
  = link_to 'Import XLS Sticker', "/stickers/import", :class=> "btn"
%p
  = link_to 'Create New Sticker', "/admin/sticker/new", :class=> "btn"

.row
  .span12
    %table.table-striped.span10
      %tr
        %th.span2 product id
        %th.span2 code
        %th.span2 activated ?
        %th.span2 verified ?
        %th.span2 actions

      - @stickers.each do |sticker|
        %tr
          %td= sticker.product_id
          %td= sticker.code
          %td= sticker.activated ? "yes" : "no"
          %td= sticker.verified ? "yes" : "no"
          %td
            = link_to 'Show', sticker
            = " - "
            = link_to 'Edit', edit_sticker_path(sticker)
            = " - "
            = link_to 'Destroy', sticker, method: :delete, data: { confirm: 'Are you sure?' }